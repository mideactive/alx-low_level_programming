#include<stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char cprog[] = "\"Programming is like building a multilinqual puzzle";

	puts(cprog);

	return (0);
}
